export default function Hanakilla() {
  return (
    <div className="page-container">
      <div className="guide-container">
        <p>
          <strong>
            Always make sure you can field a full team of the same element cards
            when pushing stages.
          </strong>{" "}
          More often than not, having fewer cards of matching elements is
          stronger than using higher-level cards that don't match the element.
        </p>
      </div>
    </div>
  );
}
